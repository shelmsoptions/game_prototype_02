<p id="notice"><%= notice %></p>

<p>
  <strong>Owner:</strong>
  <%= @character.user.first_name %> <%= @character.user.last_name %> (<%= @character.user.alias %>)
</p>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Class</th>
      <th>Hit Points</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td><%= @character.name %></td>
      <td><%= @character.class_type.name %></td>
      <td><%= @character.hit_points %></td>
    </tr>
  </tbody>
</table>
<p>
  <strong>Name:</strong>
  <%= @character.name %>
</p>

<p>
  <strong>Class:</strong>
  <%= @character.class_type.name %>
</p>

<p>
  <strong>Hit Points:</strong>
  <%= @character.hit_points %>
</p>

<%= link_to 'Edit', edit_character_path(@character) %> |
<%= link_to 'Back', user_path %>
